//Perimeter of Rectangle

//Length
const length = 20;
//Width
const width = 16;
//solved perimeter of the rectangle
p = 2*length + 2*width;

//Output
console.log(`Width: ${width}`);
console.log(`Length: ${length}`);
console.log(`Perimeter: ${p}`);